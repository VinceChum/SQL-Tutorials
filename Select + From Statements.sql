/*
Select Statement
*, 
Top, 
Distint = Unique, 
Count, 
As = naming, 
Max, 
Min, 
Avg
*/

SELECT AVG(Salary)
FROM EmployeeSalary

SELECT *
FROM EmployeeDemographics


SELECT TOP 5 Age
FROM [SQL Tutorial].dbo.EmployeeDemographics